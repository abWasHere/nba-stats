export default sortPlayersLists = (statsArr) => {
	let topPts = [];
	let topFtm = [];
	let top3pts = [];
	let topOrb = [];

	// get the 10 best => topPts
	// loop here

	console.log("topPts in function ==", topPts);

	return topPts, topFtm, top3pts, topOrb;
};
